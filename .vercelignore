# Keep Vercel deploy workspace lean. Do NOT list patterns that could match
# `public/landing2/` (app fonts live there). Root scratch dirs `LANDING2/` and
# `COPY/` are already in `.gitignore` and are not in Git — no need to repeat here.
node_modules
dist
supabase
docs
playwright-report
test-results
reports
.cursor
HEROEXTRACT
chunks
csschunks
persona agent examples context/
# Large doc exports (optional local mirrors)
docs/**

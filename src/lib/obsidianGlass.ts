/**
 * Shared obsidian frosted glass — Today KPI cards, priority feed.
 * Keep in sync visually: same tint, opacity, blur, and top-edge highlight only.
 */
export const OBSIDIAN_GLASS =
  "bg-[#0a0a0a]/50 backdrop-blur-3xl shadow-[inset_0_1px_0_rgba(255,255,255,0.15)]";
